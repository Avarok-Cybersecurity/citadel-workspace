<!-- Generated by scripts/build-gates-index.mjs. Do not edit by hand. -->
# Every gate in this repository

157 checks. Regenerate with `node scripts/build-gates-index.mjs`;
`--check` fails when this file is stale, so a new gate has to appear here.

**Read this before writing a new guard.** Four times in one campaign a check
was written that already existed elsewhere and better, because nothing listed
what was already here.

## `scripts/`

| gate | what it fails on |
|---|---|
| `check-acting-on-a-member-is-guarded.mjs` | Every server op that writes another member's standing passes the authority ladder first. |
| `check-admin-promotions-are-gated.mjs` | Every assignment that makes somebody an administrator must sit behind a gate. |
| `check-agent-binds-loopback.mjs` | The agent may only ever listen on loopback — where its network namespace is the host's. |
| `check-agent-docs-name-real-things.mjs` | The agent instructions in CLAUDE.md and .claude/agents/ are executed, not read. |
| `check-agent-docs-name-real-ui.mjs` | A UI-testing agent must not be told to click something that does not exist. |
| `check-agent-tls-matches-its-proxies.mjs` | The agent's transport, and the transport everything that fronts it dials, must be the same one. |
| `check-already-registered-predicate-matches-the-agent.mjs` | Exactly one `PeerRegisterFailure` message may be read as success. |
| `check-authorization-precedes-the-secret.mjs` | A secret comparison must not run before the caller has been authorized. |
| `check-base-image-pulls-retry.mjs` | Every step that builds images pre-pulls its base images, retried. |
| `check-bindings-match-exported-types.mjs` | Every generated binding must correspond to a type that still exists, and every exported type must have one. |
| `check-broadcasts-name-their-audience.mjs` | A push must not reach someone the matching pull would refuse. |
| `check-browser-installs-are-bounded.mjs` | Every `playwright install` in CI retries, and each attempt is bounded. |
| `check-byte-fields-do-not-print-themselves.mjs` | Every byte-carrying field in the agent's wire types must redact itself under `{:?}`. |
| `check-ci-job-timeouts.mjs` | Every CI job declares how long it may run. |
| `check-ci-liveness-recipe-reads-jobs.mjs` | No document may recommend the run-level CI liveness check. |
| `check-ci-matrices-agree.mjs` | The UI submodule's integration matrix must cover everything the parent's does. |
| `check-ci-runs-lockfile-versions.mjs` | A CI step that runs `npm install <pkg>@<version>` overrides what the lockfile resolves, so CI exercises a different version of that package than every developer and every other job. |
| `check-color-tokens-exist.mjs` | Every `bg-*` / `text-*` / `border-*` token a component uses must exist in the Tailwind colour config. |
| `check-committed-bindings-match-the-rust.mjs` | The committed ts-rs bindings must be what the Rust types generate. |
| `check-connect-in-progress-matches-the-agent.mjs` | The UI's "already connecting" test must match what the agent actually sends. |
| `check-crate-coverage.mjs` | Fails if a Cargo workspace member has no cargo-fmt, cargo-clippy or test job. |
| `check-deploy-paths-can-start-the-server.mjs` | Nothing may build or run the server image behind `deploy.sh`'s back. |
| `check-deploys-use-the-recorded-pointers.mjs` | Nothing may check submodules out with `--remote`. |
| `check-destructive-contrast.mjs` | Fails if destructive TEXT is below WCAG AA on any background it is read on. |
| `check-disconnect-reports-failure.mjs` | A disconnect that did not happen must not be reported as one. |
| `check-doc-assertions.mjs` | Checks `verify:` annotations — the escape hatch for claims that prose makes and no extractor can read. |
| `check-doc-commands.mjs` | Fails if the docs tell you to run something that does not exist. |
| `check-doc-file-refs.mjs` | Fails when a doc points at a source file that no longer exists. |
| `check-doc-setup-steps.mjs` | Fails if the stack REQUIRES configuration the quickstart never tells you to create. |
| `check-docker-manifest-matches-the-crate.mjs` | A Docker build that REPLACES a crate's Cargo.toml must declare the same runtime dependencies it does. |
| `check-docker-workspace-manifest.mjs` | The production server image builds against a SUBSTITUTE root manifest. |
| `check-dockerfiles-copy-every-crate.mjs` | Every Rust workspace member must be COPY'd into every image that runs cargo. |
| `check-docs-are-listed-in-the-index.mjs` | Every document in `docs/` must be reachable from `docs/README.md`. |
| `check-docs-name-real-symbols.mjs` | CLAUDE.md and ARCHITECTURE.md are loaded into every session. |
| `check-docs-name-the-real-ui-port.mjs` | Every document that tells someone where the UI is must name the port it is actually on. |
| `check-envsubst-vars-are-validated.mjs` | Every variable envsubst is allowed to substitute must be validated first. |
| `check-esm-imports-have-extensions.mjs` | In an ESM package, a relative import that runs must name a file Node can find. |
| `check-every-gate-is-invoked.mjs` | Every `check-*.mjs` must be invoked by something. |
| `check-every-integration-spec-has-a-runner.mjs` | Every integration spec must be named by an npm script that runs it. |
| `check-expensive-jobs-wait-for-cheap-ones.mjs` | A job that starts Docker must not run before every cheap gate has passed. |
| `check-failure-capture-covers-the-p2p-log.mjs` | The failure-time log capture must be able to match every P2P log line the agent emits. |
| `check-feature-gated-tests-are-compiled.mjs` | A test behind a cargo feature CI does not enable is not skipped. |
| `check-file-length.mjs` | The 250-line cap on UI source files. |
| `check-gates-have-their-dependencies.mjs` | A gate that imports a package must run in a job that installs one. |
| `check-gates-run-on-the-oldest-node.mjs` | A gate may not use an API newer than the Node this repository supports. |
| `check-gates-say-what-they-examined.mjs` | A gate that passes must say what it examined. |
| `check-generated-artefacts-present.mjs` | The checks that follow consume generated artefacts. |
| `check-generated-types-are-all-exported.mjs` | A generated type nothing exports is a type nobody can import. |
| `check-generated-types-fresh.mjs` | The hand-copied TypeScript bindings must match the generated ones. |
| `check-git-deps-agree-across-lockfiles.mjs` | Two Cargo workspaces that build halves of one running system must pin every shared git dependency to the same revision. |
| `check-group-handlers-are-authorized.mjs` | Every group-chat request handler must ask a group authorization gate first. |
| `check-handlers-cannot-panic.mjs` | A panic in a request handler is a request that hangs for ever. |
| `check-hover-only-controls.mjs` | No control may be revealed by hover alone. |
| `check-icon-button-names.mjs` | Fails if an icon-only Button has no accessible name. |
| `check-image-fetches-retry.mjs` | A network fetch in an image build is retried. |
| `check-images-build-what-the-lockfile-says.mjs` | An image that compiles this workspace must copy the lockfile. |
| `check-install-doc-names-required-env.mjs` | `docker compose -f docker-compose.production.yml up -d --wait` fails with no useful message when a variable that has no default is unset: the container starts, the binary exits, and `--wait` reports a timeout. |
| `check-intent-results-checked.mjs` | An intent whose result reports failure must have that result read. |
| `check-lint-scripts-match-ci.mjs` | CI lints every workspace with `--max-warnings 0`. |
| `check-listener-fanouts-are-isolated.mjs` | A fan-out over listeners must not let one of them silence the rest. |
| `check-localdb-responses-are-classified.mjs` | A LocalDB response must be classified by its VARIANT, never by its presence. |
| `check-log-arguments-are-cheap-in-rust.mjs` | Work whose only consumer is a log line must be behind `log_enabled!`. |
| `check-message-storage-has-one-owner.mjs` | A room's message keys are built in exactly one module. |
| `check-network-fetches-are-bounded.mjs` | A retry bounds failures. |
| `check-nginx-headers-are-complete.mjs` | Every nginx location that serves content must carry the same headers as `/`. |
| `check-no-nested-npm-ci-in-workspaces.mjs` | `npm ci` inside a directory that is already a root workspace re-resolves that subtree and unhoists the root's devDependencies. |
| `check-no-test-features-shipped.mjs` | No shipped binary may be built with a test-only cargo feature. |
| `check-node-content-is-not-broadcast-to-everyone.mjs` | Node content must not be broadcast to every connected socket. |
| `check-node-floor-is-enforced.mjs` | The Node floor must be enforced, not merely declared. |
| `check-peer-list-timeout-parity.mjs` | The browser must not give up on peer discovery before the agent does. |
| `check-permission-parity.mjs` | The Permission enum exists twice: authoritatively in Rust (citadel-workspace-types) and mirrored in TypeScript for the admin UI. |
| `check-preconditions-precede-the-branch.mjs` | A step every path needs must run before the path is chosen. |
| `check-preflight-runs-what-ci-runs.mjs` | Preflight must invoke each gate the way CI invokes it — arguments included. |
| `check-preview-csp-matches-production.mjs` | The CSP `npm run preview` serves must be the one nginx serves. |
| `check-production-backend-is-safe.mjs` | Production must not select a backend that loses writes. |
| `check-production-image.mjs` | Drives a real browser against the real production image. |
| `check-readiness-markers-are-printed.mjs` | A "wait until you see this line" marker must be a line something prints. |
| `check-remote-checks-read-the-deployment.mjs` | A script that reaches the production host must read that host's configuration, not a literal it was written next to. |
| `check-request-paths-do-not-wait-forever.mjs` | A request handler must not await something with no bound. |
| `check-required-env-is-documented.mjs` | Every value production REQUIRES from `.env` must be in `.env.example`. |
| `check-responsive-label-loss.mjs` | A control's only text may not be hidden by a responsive `hidden` class. |
| `check-restart-policies.mjs` | Every long-running service must declare a restart policy. |
| `check-rust-modules-are-compiled.mjs` | Every .rs file under a crate's src/ must be reachable by a `mod` declaration. |
| `check-secrets-are-compared-in-constant-time.mjs` | `String == String` in Rust compares lengths and then runs `memcmp`, which returns as soon as two bytes differ. |
| `check-sender-identity.mjs` | A message's sender must come from the transport, never from its payload. |
| `check-service-logs-are-captured.mjs` | A job that starts the stack must dump its logs when a test fails. |
| `check-session-query-failures-are-not-absence.mjs` | "I could not ask" is not "there is nothing there". |
| `check-session-teardown-prunes-cid-state.mjs` | Every place a session leaves `server_connection_map` must also prune the CID-keyed kernel maps (pending peer signals, username cache). |
| `check-sessions-are-removed-in-two-places.mjs` | A session outlives its connection. |
| `check-setup-node-restores-the-npm-cache.mjs` | Every `actions/setup-node` step must restore the npm cache. |
| `check-stack-reachable.mjs` | Verify the running stack is reachable FROM THIS MACHINE, not from inside a container. |
| `check-static-assets-are-compressed-well.mjs` | Static assets must be compressed at a level worth the bytes. |
| `check-storage-keys.mjs` | Fails if the UI reads a storage key nothing ever writes. |
| `check-submodule-gate-judges-a-worktree.mjs` | The submodule-pointer guard must give the same verdict from a linked worktree as from the primary checkout. |
| `check-submodule-pointers-pushed.mjs` | Every submodule pointer this repo records must exist on that submodule's remote. |
| `check-submodule-work-is-committed.mjs` | A submodule must not carry uncommitted changes to tracked files. |
| `check-submodules-are-populated.mjs` | A clone without `--recurse-submodules` cannot build, and does not say so. |
| `check-the-loopback-agent-origin-is-wired.mjs` | The hosted UI's one off-origin socket is wired end to end, or not claimed. |
| `check-third-party-images-are-pinned.mjs` | A third-party image in a compose file must name a version, not float. |
| `check-token-contrast.mjs` | Every semantic foreground must clear WCAG AA on the surface it names. |
| `check-transfer-cap-parity.mjs` | The inline-upload byte cap is declared in two languages and nothing bound them. |
| `check-wasm-matches-its-source.mjs` | The committed WASM binary must have been built from the committed source. |
| `check-wasm-rebuild-triggers-match-the-stamp.mjs` | What triggers a WASM rebuild must be what the staleness stamp covers. |
| `check-wire-types-match-the-rust.mjs` | Every Rust type marked for export must have a generated TypeScript file, and every generated file must correspond to one. |
| `check-workflow-npm-scripts-exist.mjs` | Every `npm run X` in the workflow must name a script that exists where it runs. |
| `check-workflows-parse.mjs` | Every workflow file must be valid YAML. |
| `check-workspace-missing-parity.mjs` | "No workspace found" is a sentinel the client matches on, written by a server that does not know it is one. |

## `citadel-workspaces/scripts/`

| gate | what it fails on |
|---|---|
| `check-a-promise-is-not-a-boolean.mjs` | No declared type unions a boolean with a Promise. |
| `check-accessibility.mjs` | axe against the screens a visitor meets before they have an account. |
| `check-agent-down.mjs` | What a first-run user sees before they have started the agent. |
| `check-already-registered-is-not-a-failure.mjs` | Every consumer of `PeerRegisterFailure` must treat "already registered" as success. |
| `check-assertions-can-fail.mjs` | An assertion no input could falsify is worse than no assertion: it reports a guarantee it does not provide. |
| `check-baselines-are-committed.mjs` | A gate that rewrites its own baseline, left uncommitted, stops being a gate. |
| `check-bundle-budget.mjs` | Fails the build if the landing route's critical path grows past its budget. |
| `check-captured-is-not-failed.mjs` | What a spec CAPTURES from the console is not what it counts as a FAILURE. |
| `check-cid-is-bigint.mjs` | A CID is a `bigint`, and a declared field that names one may not be a string. |
| `check-clickables-are-keyboard-reachable.mjs` | A clickable element a keyboard can reach. |
| `check-components-are-mounted.mjs` | A component nobody renders, kept alive by its own test. |
| `check-connect-awaiters-handle-every-answer.mjs` | A Connect has three terminal answers. |
| `check-console-printers.mjs` | No console listener truncates the message itself. |
| `check-controls-are-addressed-by-testid.mjs` | A control you CLICK must not be found by the words on it. |
| `check-controls-are-wired.mjs` | A form control that stores nothing must not look like it stores something. |
| `check-debug-args-are-cheap.mjs` | A log line that costs nothing to print must cost nothing to build. |
| `check-errors-are-not-swallowed.mjs` | No production catch discards its error without saying why. |
| `check-event-listeners-have-emitters.mjs` | A listener and an emitter are two ends of one mechanism. |
| `check-event-setups-are-covered.mjs` | Every `use*EventSetup` hook must appear in the unsubscribe test's table. |
| `check-every-gate-is-invoked.mjs` | A gate nobody runs is not a gate. |
| `check-every-test-runs.mjs` | Every unit-test file on disk is one vitest actually collects. |
| `check-explicit-types.mjs` | Every declaration carries a type, and the count can only go down. |
| `check-forced-clicks-are-justified.mjs` | A forced click in the responsive suite hides what that suite measures. |
| `check-gates-pin-their-world.mjs` | Every browser-driving gate must pin the world it measures. |
| `check-group-permissions-are-enforced.mjs` | Every permission the group role editor offers must be read somewhere. |
| `check-helpers-survive-a-vanishing-target.mjs` | A helper must not die because the thing it was closing closed itself. |
| `check-inert-props.mjs` | A prop that is declared, styled, destructured -- and never passed. |
| `check-installers-are-called.mjs` | A `bind…`/`start…` function nothing calls. |
| `check-lighthouse.mjs` | Lighthouse gate for the production build. |
| `check-member-lists-name-their-domain.mjs` | Every subscriber to a member list must check which domain the list is for. |
| `check-mobile-layout.mjs` | Checks the first-run screens at phone width for the two failures that make an installed app feel broken on a phone, neither of which axe reports. |
| `check-modules-are-imported.mjs` | Every module under src/ is reached by an import. |
| `check-one-answer-to-who-am-i.mjs` | "Which session is this?" is answered in one place. |
| `check-parent-checkouts-agree-on-the-ref.mjs` | Every job that checks the parent repository out must take the ref the `parent-ref` job resolved — and must declare that it depends on it. |
| `check-peer-names-have-one-answer.mjs` | How a peer is named is decided in one module, not restated inline. |
| `check-permission-gates.mjs` | A control's enabled state comes from `usePermission`, not from `hasPermission`. |
| `check-presence-is-not-invented.mjs` | Nobody invents a peer's online status. |
| `check-pwa-installable.mjs` | Fails the build if the app would stop being installable. |
| `check-pwa-offline.mjs` | Verifies the promise the app makes when it says "Citadel has been installed and will now load without a connection". |
| `check-pwa-update.mjs` | Verifies the promise the app makes when it says "A new version of Citadel is ready" — that an ALREADY-INSTALLED app notices a deployment and offers the user the reload, rather than serving yesterday's bundle forever. |
| `check-reduced-motion.mjs` | Verifies the app honours prefers-reduced-motion, and that honouring it did not break the thing that depends on animations existing. |
| `check-source-maps.mjs` | Every emitted JS chunk ships a usable source map. |
| `check-specs-search-for-real-copy.mjs` | A spec may not search for words the app does not contain. |
| `check-success-flags-are-checked.mjs` | A function that answers "did that work?" whose answer nobody reads. |
| `check-test-failures-capture-backend-logs.mjs` | A job that starts the stack and then runs tests must dump the backend logs when a TEST fails, not only when the stack fails to start. |
| `check-testids-exist.mjs` | Every `data-testid` a spec addresses is rendered by the app. |
| `check-the-needle-is-folded-once.mjs` | Filtering a collection must not fold the query once per candidate. |
| `check-toast-clears-header.mjs` | On a phone, a toast must not land on the fixed header. |
| `check-user-facing-errors.mjs` | A user is never shown a raw thrown value. |
| `check-wire-fields-exist.mjs` | Reading a field the wire message does not have. |
