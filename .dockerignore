# Git files
.git
.gitignore

# Docker related files
Dockerfile
.dockerignore
docker-compose.yml

# Tilt file
Tiltfile

# Rust build artifacts
target_ide/
target/
**/target/

# Cargo.lock is NOT ignored, deliberately.
#
# It was, with the rationale "let Docker resolve its own deps to avoid stale git
# revision hashes". That is precisely the failure mode, wearing the language of a
# fix: `citadel_sdk` is a git dependency on `branch = "master"`, so resolving
# fresh means the image builds whatever that branch happens to be at the moment
# the image is built.
#
# A "stale" revision is the one that was tested. Freshness is how a change merged
# to another repository arrives here with no commit, no review and no signal —
# which happened: a protocol PR landed between two CI runs of an unchanged branch
# and turned an integration test red, and only a passing-versus-failing log
# comparison could have found it.
#
# Upgrading the protocol is now a commit that moves Cargo.lock, which is what an
# upgrade should look like.

# Node.js dependencies (assuming they are installed within the container or managed differently)
node_modules/
**/node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Tauri build artifacts (often within src-tauri/target)
src-tauri/target/
src-tauri/gen/
src-tauri/node_modules/

# General build/dist folders (add specific ones if you use them)
dist/
build/
out/

# Logs
*.log
logs/
*.log.*

# OS-specific files
.DS_Store
Thumbs.db

# Editor/IDE specific files
.vscode/
.idea/
*.sublime-project
*.sublime-workspace

# Environment files (if sensitive or managed outside image)
.env
.env.*
!/.env.example # Keep example files if they exist

# Temporary files
*.tmp
*.swp
*~

# Test reports (if generated)
coverage/
junit.xml

# Playwright MCP test artifacts (avoid triggering Tilt rebuilds during testing)
.playwright-mcp/
.pr-reviews/

# Specific workspace files (add if necessary)
.service-pid
.server-pid
internal-service.log
citadel-server.log

# UI directories - NOTE: citadel-workspaces is needed for UI container build
# citadel-workspaces/
# citadel-workspace-client-ts/
# wasm-client-ts/
test_*.html
test_*.js
.claude

# Volume backups. scripts/backup-volumes.sh defaults here, and these archives
# contain internal_service_data -- every user's account keys, which INSTALL.md
# states are unrecoverable and held nowhere else. commit.sh runs `git add
# --all`, so an untracked tarball of them is one command from being committed.
backups/
