import { RegistrationC2S } from '@common/types/registrationc2s';
import invokeApi from '../utils/invoke-api';

type ReturnType = {};

// const registeC2s = async (): Promise<RegistrationC2S> => {
//   const { data } = await invokeApi<ReturnType>({});
//   // return data;
// };
