import { Meta, StoryFn } from '@storybook/react';
import { Header as Component } from './Header';

export default {
  title: 'Components / Header',
} as Meta;

export const Header: StoryFn = () => <Component></Component>;
